---
name: peachflow:config
description: Reconfigure an existing peachflow project. Alias for /peachflow:init when project is already initialized.
allowed-tools: Read, Write, Bash, Glob, AskUserQuestion
---

# /peachflow:config

This is an alias for `/peachflow:init`. When called on an initialized project, it enters configuration mode.

See `/peachflow:init` for full documentation.
